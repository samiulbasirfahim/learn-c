#include <stdio.h>
#include <math.h>
// instructions and operators

// main funcion
int main()
{
	double a, b, c;
	a = b = c = 4;

	int e = 4, f = 5, g;
	g = 1;
	// printf("value of %d, value of %d, value of %d", a, b, c);

	/* value iwll be assigned last variable y, not in x */
	int x, y = a + g;
	/* value will be assigned in first vairable, j will declare but its value isnt assign */
	int l = 2, j;
	/* 3 to the power 2 */
	int p = pow(3, 2);

	printf("value of e is %d, value of f is %d and value of g is %d", e, f, g);

	printf("\n p is %d", p);

	return 0;
}

/*
	first 8,/,%  then +,-
	left to right
	bracate first

	@same like math

 */
